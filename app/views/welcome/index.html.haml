%table.table
  %thead
    %tr
      %th Ürün Resmi
      %th Ürün Adı
      %th Fiyatı (TL/m2)
      %th Kategorisi
  %tbody
  - @products.each do |p|
    %tr
      %td= link_to (image_tag p.image.url(:medium)), product_path(p)
      %td= link_to p.name, product_path(p)
      %td= p.price
      %td= p.category.name
