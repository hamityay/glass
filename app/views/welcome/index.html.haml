%table.table
  %thead
    %tr
      %th Ürün Resmi
      %th Ürün Adı
      %th Fiyatı (TL)
      %th Kategorisi
  %tbody
  - @products.each do |p|
    %tr
      %td
      %td= link_to p.name, product_path(p)
      %td= p.price
      %td= p.category.name
